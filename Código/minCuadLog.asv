function w = minCuadLog(M)

L= log(M);
s=size(M);
long= (s(1)*s(2))-s(1);
H = zeros([long,s(2)]);
b=zeros([long,1]);
k=1;
for i= (1:s(1)), for j=1:s(2),
    if (i ~= j)
     H(i,i)=1;
     H(i,j)=-1;
     b(k)=L(i,j);
     k=k+1;
    end
    end,end


end
